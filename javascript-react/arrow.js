// let fruits = (f) => {
//   return f;
// };

// ! we can do it like this if the function is simple
let fruits = (f) => f;

console.log(fruits("Apple"));

let multiplication = (m) => m * m * m;
console.log(multiplication(3));

let add = (m, n) => m + n;

console.log(add(3, 5));
